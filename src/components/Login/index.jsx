import React, { useState } from 'react';

export default function Login() {
    
    const [open, setOpen] = useState(false);

    return (
        <div style={{position: 'fixed', bottom: 0, right: 0, width: '50px', height: '50px', borderRadius: '50%', backgroundColor: '#444'}}>
            
        </div>
    )
}
